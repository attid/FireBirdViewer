# 009: Fix browser tab title

## Контекст
На странице в браузере отображался дефолтный title FastHTML вместо ожидаемого `FireBird Viewer`.

## План изменений
1. [x] Найти, где формируется `<title>` для ключевых маршрутов
2. [x] Зафиксировать title как `FireBird Viewer` для `/`, `/connect` (ошибки) и `/dashboard`
3. [x] Не менять подпись в UI (navbar/div)
4. [x] Запустить `just check`

## Риски и открытые вопросы
- Нужно подтвердить, требуется ли единый title на всех HTMX-фрагментах, или достаточно основных full-page роутов.

## Верификация
- Открыть `/` и `/dashboard` в браузере и проверить, что title вкладки: `FireBird Viewer`.
- Для error-состояния `/connect` проверить тот же title.
